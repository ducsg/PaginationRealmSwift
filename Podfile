# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PaginationRealmSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RealmSwift'
  # Pods for PaginationRealmSwift

  target 'PaginationRealmSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PaginationRealmSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
